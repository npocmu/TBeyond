/////////////////////////////////////////////////////////////////////
function CulturePointsInfo(cp)
{
   this.cp = cp; 
   this.ttUpd = undefined; // date of information update (server timestamp when residence/palace last visited)

   return this;
}
